# Memory Index — feature_flag

*Loaded automatically at the start of every session. Keep entries to one line each.
Updated by `/save-memory`. See `README.md` for how this system works.*

<!-- Format: - [Title](path) — one-line hook. Newest relevant entries near the top. -->

- [CD: deploy prod via Docker Compose on the self-hosted runner](decisions/0022-cd-deploy-prod-self-hosted-compose.md) — added a `deploy` job to `workflow.yml` (same file, NOT a separate cd.yml — `needs: publish` only works in-workflow; a separate trigger would race publish and pull a not-yet-pushed image). Trigger: **push to `develop`** (continuous deploy) + `v*` tag + manual `workflow_dispatch`; chain `test`(ubuntu)→`publish`(ubuntu)→`deploy`(self-hosted), each gated on `needs: publish`. Self-hosted safe: maintainer-only refs (never fork; repo public so CI test/publish STAY on ubuntu). **Deploy pulls the immutable `sha-<short>` tag** (`sha-${GITHUB_SHA:0:7}`, matches metadata-action `type=sha`), NOT the moving `develop` tag → traceable + deterministic rollback (dispatch with old `sha-`); tag→`{{version}}`, dispatch→input verbatim; shell-derived (env-injected, no `${{ }}`), lowercased. **Concurrency fix:** top-level `cancel-in-progress: ${{ github.event_name == 'pull_request' }}` (cancel PR CI only; push/tag/dispatch never cancelled mid-`compose up`); deploys serialize via job-level `deploy-prod` group. **Auto-rollback:** capture running image via `docker inspect ff_app` → `$GITHUB_ENV`; `if: failure()` step restores it + re-verifies + keeps job RED (skips first deploy / prev==new). Limitation: app-image only, forward Liquibase migration NOT reverted. New **`docker-compose.prod.yml`**: `image: ${APP_IMAGE}` (pull, no build), Postgres port NOT host-published, all secrets `${VAR:?}`-required. Health-gate polls `/actuator/health/readiness` ~150s then dumps logs+fails. **Prereqs (set 2026-08-23):** `production` env + secrets `APP_JWT_SECRET`/`POSTGRES_*`; runner needs `docker compose` v2 (**still unverified**). Deploy auto on merge/push to `develop`.
- [SonarQube analysis in CI on a self-hosted runner](decisions/0018-sonarqube-ci-self-hosted-runner.md) — `.github/workflows/sonar.yml`: `./mvnw verify …:sonar` on `runs-on: self-hosted` (runner on same network as the internal SonarQube server). The workflow IS the integration — no `.env`, no GitHub↔Sonar "link"; only 2 **repo secrets** `SONAR_TOKEN`+`SONAR_HOST_URL`. `sonar-maven-plugin` auto-reads those env vars → dropped redundant `-Dsonar.host.url/-Dsonar.token` (kept `-Dsonar.projectKey`). Hardening: `permissions: contents:read`, `timeout-minutes:20` (1 self-hosted runner = 1 job), fork-PR guard (no secrets + never run untrusted fork code on self-hosted), `fetch-depth:0`, `~/.sonar/cache`. Runner: dedicated non-root user + `svc.sh` service (survives reboot; `run.sh` dies with terminal). Coverage (JaCoCo `jacoco.xml`) is a `pom.xml` concern, NOT verified here
- [Percentage rollout: fail-open contract + Math.abs sign bug](decisions/0021-percentage-rollout-contract-issue-35.md) — issue #35: ~90% was ALREADY on develop (landed via `feat/rollout-percent`, issue never closed). Real work = **ADR-0004** fail-open contract (no identifier ⇒ flag fully on, so a rollout is NOT access control; fail-closed/400 rejected as breaking), a real bug (`Math.abs(Integer.MIN_VALUE)` is still negative and `MIN_VALUE % 100 == -48` ⇒ negative bucket ⇒ that identifier permanently included even at 1%; fixed with `hash & Integer.MAX_VALUE`, unreachable via the public API so needed a package-private `toBucket` seam), and the missing statistical ACs (no `RolloutEvaluatorTest` existed): determinism, **monotonicity**, chi-square uniformity over 10k identifiers, per-flagKey bucket independence. Caching bullet already satisfied by PR #53 (caches pre-rollout `FlagStateSnapshot` by envId, evaluates per request) — recorded as an invariant for future cache layers. Note: SDK `enabled` now means *effective per identifier*, not raw state
- [Verify an issue's scope against the code first](conventions/stale-issue-scope-verify-before-implementing.md) — issue bodies go stale here: work lands via side branches without closing the issue, so an issue can read unimplemented when most of it shipped. Grep each scope bullet (migrations dir, field wiring, is the util actually called, do the AC's tests exist, `git log` the paths) and report the delta instead of implementing the issue as written
- [Docker: port alignment + non-root + compose app service](decisions/0019-docker-port-nonroot.md) — issue #27: EXPOSE 8080→8081 (=server.port), non-root `spring` user (USER), add `app` service to docker-compose (DB via service name). At the develop merge: kept #25's readiness HEALTHCHECK on top of `USER spring`, and the compose `app` service must now pass `APP_JWT_SECRET` because the image bakes `SPRING_PROFILES_ACTIVE=prod` (no dev fallbacks)
- [Windows docs case-collision (ARCHITECTURE.md vs architecture.md)](conventions/windows-docs-case-collision.md) — two git files differ only by case → permanently-dirty tree on Windows; never `git add -A`, stage explicit paths; resolved on develop by renaming the uppercase file to `docs/architecture-design-v1.md`
- [GHCR publish + Trivy gate: scan-before-push](decisions/0018-docker-publish-trivy-gate-issue-34.md) — issue #34: `publish` job in workflow.yml builds local (`load:true`, no push) → Trivy scans that local image (HIGH/CRITICAL gate, `.trivyignore`) → only a clean scan gets pushed, reusing the `type=gha` cache. Scan by first tag (`fromJSON(steps.meta.outputs.json).tags[0]`), not `imageid` — undocumented pattern. **Real bug caught by review**: `type=raw,value=latest,enable={{is_default_branch}}` is always false here — this repo's actual GitHub default branch is `develop` (Gitflow), not `main`, and `publish` never runs on `develop` — so `latest` never applied and a bare `main` push would fail (empty tag list). Fixed with an explicit `enable=${{ github.ref == 'refs/heads/main' }}`. Also bumped `jacoco.line.coverage` 0.83→0.87 (measured actual 0.89, stale comment)
- [Append-only audit log for admin mutations](decisions/0020-audit-log-flag-org-mutations.md) — issue #31: `AuditService.record()` called SYNCHRONOUSLY inside each mutation's own `@Transactional` (atomic, exactly-one-row — NOT the async AFTER_COMMIT Slack pattern). before/after = response DTO → `Map` (reuses toResponse, so no secrets); env create audits non-secret `EnvironmentResponse`, key rotation logs before/after=null (never the key, tested). JSON via `@JdbcTypeCode(SqlTypes.JSON)`+`Map`, Liquibase `${json.type}`=jsonb/json — round-trip validated on real H2. **Boot 4.1 has NO ObjectMapper bean in webEnvironment=NONE** → AuditService builds its own. Read `GET /organisations/{orgId}/audit-log` newest-first, VIEWER+, paginated (reuses #33). audit_log append-only, NO FKs (outlives entities). 16 audit points. **Migration 011** (renumbered from 010 at the develop merge — #32 took 010 for refresh-tokens; the `changeSet id` follows the filename so both moved). Was STACKED on #33; merged with develop, keeping develop's `@Transactional(readOnly=true)` on `listMembers` (the #52 fix). Enum entityId for FLAG_STATE = state id
- [Refresh-token family revoke: transaction semantics](decisions/0017-refresh-token-family-revoke-transaction-semantics.md) — issue #32: a security control that revokes-then-throws is silently undone by the rollback (unchecked exception). `noRollbackFor` fixes only the innermost tx → any `@Transactional` caller reopens the hole; real fix is a **separate `REQUIRES_NEW` bean** (`RefreshTokenFamilyRevoker`) that commits independently. Also: never revoke while `consume()` holds a row lock — the `REQUIRES_NEW` revoke self-deadlocks on the locked row (DB can't detect it; PG hangs, H2 hides it), so check `user.enabled` BEFORE consume. Mock `verify()` proves *called* not *committed* → needs a non-`@Transactional` integration test
- [Offset pagination on admin list endpoints](decisions/0017-pagination-admin-list-endpoints.md) — issue #33: Spring Data `Pageable` on all 6 admin list endpoints (SDK eval EXCLUDED). Controller `@ParameterObject @PageableDefault(size=20, sort={createdAt,id})` → service `Page<Resp>` → repo paginated method. Hand-rolled `PageResponse{content,page,size,totalElements,totalPages}` (breaking: bare array→envelope). Max-100 clamp via `PaginationConfig` `PageableHandlerMethodArgumentResolverCustomizer` bean (constants reused in standalone tests). `listMine`=findByIdIn(ids,pageable), empty→Page.empty. `EnvironmentRepository.findAllByProjectId` OVERLOADED (List for flag-create + Page for list). ADR-0003. **Review flagged #52 root cause**: `listMembers` reads lazy `getUser().getEmail()` outside tx (open-in-view=false) → LazyInit 500; pre-existing, fix in #52 with `@Transactional(readOnly=true)`/JOIN FETCH
- [Secrets externalization: bind-time validation + no-default prod profile](decisions/0016-secrets-externalization-fail-fast.md) — issue #23: JwtProperties bean validation fails startup on missing/short/placeholder/low-entropy secret; prod profile file has NO defaults so dev values can't leak; Dockerfile bakes SPRING_PROFILES_ACTIVE=prod; a prod-only EnvironmentPostProcessor names any missing/blank SPRING_DATASOURCE_* var at startup (the binder passes `${VAR}` through as a literal, so DataSourceProperties alone failed later with an obscure Hikari error). Was 0008 on-branch → renumbered 0016 at merge
- [@ConfigurationProperties validation gotchas (Boot 4.1)](conventions/springboot-configprops-binding-gotchas.md) — binder passes unresolved `${VAR}` through as literals (unlike @Value); @AssertTrue only fires on getter-shaped isXxx() methods on records; @AssertTrue getters keep secrets out of the bind-failure report (field constraints echo the value)
- [Structured JSON logging + request correlation](decisions/0015-structured-json-logging-request-correlation.md) — issue #28: `RequestCorrelationFilter` (plain filter registered via `FilterRegistrationBean` at HIGHEST_PRECEDENCE → before the single `FilterChainProxy` @−100, i.e. before all 3 security chains) sets `requestId` (MDC + `X-Request-Id` header + request attr) and clears the WHOLE MDC in `finally` = single choke point; auth filters add `userId`/`envId` with no own cleanup. Incoming `X-Request-Id` sanitized `[A-Za-z0-9_-]{1,64}` (log-forging). `GlobalExceptionHandler` stamps `requestId` (only when non-null). `logback-spring.xml`: prod=LogstashEncoder JSON, dev=`[req=%X{requestId}]` pattern; dep `logstash-logback-encoder:8.0` runtime. No async in controllers → OncePerRequestFilter async caveat N/A
- [Code graph adoption: ArchUnit + CodeGraphContext MCP](decisions/0014-codegraph-adoption.md) — 2 goals→2 tools: **ArchUnit** (bytecode-exact governance in `mvn verify`, Tier-1 7 rules / Tier-2 custom conditions / Tier-3 data-flow it CAN'T prove → jQAssistant later) + **CodeGraphContext MCP** (agent queries, tree-sitter *approximate* Java edges, Python 3.12). ArchUnit tests are static-only → Boot-4.1 test landmines DON'T apply; use FreezingArchRule for pre-existing violations. Spec `docs/specs/codegraph-adoption.md` (Mermaid diagram, `linkStyle` is positional). Issues #48(M/5h)→#50→#49; jQAssistant/Joern deferred, Sourcegraph/build-own rejected
- [issue-board.sh estimate fails on first run (eventual consistency)](conventions/issue-board-estimate-eventual-consistency.md) — `ensure_item` adds the card then re-lists immediately; GitHub Projects API isn't consistent yet → `could not locate or create board item` on run 1 for a NEW issue. Card IS added; just re-run. Fix later: retry/sleep or read id from `item-add --format json`
- [Micrometer + Prometheus metrics](decisions/0012-micrometer-prometheus-metrics.md) — issue #29: self-contained actuator+prometheus (didn't wait for #25), `/actuator/prometheus` behind @Order(0) mgmt chain (health public, else HTTP-Basic METRICS role, local in-memory user), bounded meters `ff_evaluations_total{environment}`+timer / `ff_flag_changes_total` / `ff_auth_failures_total`, eager-registered at 0. Merged with #25: mgmt chain now owns all /actuator/** (superseded #25's admin-chain health permitAll). PR #44 review: env-tag cardinality is platform-wide (all orgs), not per-tenant — Javadoc corrected (`6eb7d28`)
- [Actuator mgmt chain on Boot 4.1 (2 gotchas)](conventions/actuator-management-chain-boot41.md) — `EndpointRequest`/`HealthEndpoint` moved to the `spring-boot-security` module in 4.1 → match `/actuator/**` paths instead; blank `{noop}` shared secret = auth BYPASS (empty-vs-empty matches) → build the account `.disabled(true)` when the secret is blank
- [Slack notifications (in-app + harness + GitHub app)](decisions/0013-slack-notifications.md) — 3 mechanisms per docs/slack-notifications.md: GitHub Slack app (CI/PR/release, no code), issue-board.sh `ready` webhook (opt-in via SLACK_WEBHOOK_URL), and in-app events (flag toggle/archive/key-rotation) via `@Async @TransactionalEventListener(AFTER_COMMIT)` → fail-safe SlackNotifier. Key never in rotation event; catch logs exception CLASS not message (URL-in-message leak); enabled=false default. Also: **Java 21 + Maven now ON this machine** (0012's "no JDK" note is stale)
- [Spotless scoping + macOS bash 3.2 hook gotchas](conventions/spotless-scoping-and-bash32.md) — `-DspotlessFiles` matches the ABSOLUTE path (relative regex silently formats nothing → prefix repo root); macOS `.githooks` run under bash 3.2 (no mapfile/readarray → stay array-free); "staged==working" is not proof the formatter ran (assert on content + `git show :path`)
- [Harness guards + Spotless + coverage-floor](decisions/0012-harness-guards-spotless-coverage.md) — added 4 enforcement gaps: PreToolUse hook blocks edits to existing Liquibase changesets; Stop hook nudges /security-review on sensitive-path changes; Spotless (google-java-format) `spotless:check` bound to verify + Stop-hook auto-format; coverage-floor.sh to lock the JaCoCo ratchet. Rollout: run `./mvnw spotless:apply` + `coverage-floor.sh --apply` once (no Java on dev machine yet)
- [Project-aware review-pr skill](decisions/0011-review-pr-skill.md) — forks official /code-review (plain MD prompt file): keeps 5-agent + 0–100 confidence <80 filter, adds a 6th reviewer for repo sensitive areas (security chains, immutable key, Liquibase, PermissionService, ApiKeyGenerator); two modes (local diff / PR#+gh comment); fork≠inherit, re-sync on upstream change
- [Actuator health/liveness/readiness endpoints](decisions/0010-actuator-health-endpoints.md) — issue #25: expose only health+info, only /actuator/health/** anonymous (info+rest authenticated), readiness includes db, liveness process-only, show-details=never; Dockerfile HEALTHCHECK on readiness (port 8081)
- [permitAll() does not skip servlet filters](conventions/permitall-does-not-skip-servlet-filters.md) — issue #25: permitAll only relaxes authorization; custom filters on the same chain still run for that path — a new anonymous path (e.g. /actuator/health/**) is safe only because AuthRateLimitFilter self-skips non-/auth and JwtFilter no-ops without a Bearer
- [Rate limiting with Bucket4j](decisions/0009-rate-limiting-bucket4j.md) — issue #26: in-memory token buckets, per-IP on /auth/** (getRemoteAddr, no XFF), per-env-id on /sdk/**, 429+Retry-After ProblemDetail, Caffeine-evicted buckets; invalid-key SDK traffic unthrottled (follow-up)
- [SecurityFilter order must anchor on a standard filter](conventions/spring-security-filter-order-anchor.md) — issue #26: addFilterBefore/After on a custom filter → "does not have a registered order" fails every context; anchor on UsernamePasswordAuthenticationFilter
- [2nd @SpringBootTest context collides on shared H2](conventions/second-springboottest-context-shared-h2.md) — issue #26: distinct @SpringBootTest(properties) = 2nd context re-runs Liquibase on mem:testdb → "DATABASECHANGELOG already exists"; give it its own DB URL
- [Hash SDK API keys at rest (SHA-256)](decisions/0008-hash-sdk-api-keys-at-rest.md) — issue #24: store unsalted SHA-256 hex (keys are 256-bit random), plaintext one-time reveal via dedicated EnvironmentSecretResponse (breaking), last_used_at throttled ~5min; migration 009 backfills via pgcrypto
- [Postgres-only migrations must be guarded on H2](conventions/liquibase-postgres-only-migrations-on-h2.md) — tests run the full changelog on H2 (PostgreSQL mode); wrap pgcrypto/extension SQL in `dbms="postgresql"` (empty table → backfill skip is safe); verify Postgres/Java hash parity with a pinned vector
- [SDK eval endpoint 500s on H2 (reserved `key` column)](conventions/sdk-eval-key-column-h2-500.md) — issue #24: GET /api/v1/sdk/flags 500s on H2 (`Column "ff1_0.key" not found`); pre-existing/H2-only; for auth tests assert "not 401" not 200
- [JWT filter: catch UsernameNotFoundException + JwtException; warn not debug](conventions/jwt-filter-catch-scope.md) — issue #10: two separate parseSignedClaims() calls create a TOCTOU gap; catch both; valid-token/missing-subject is log.warn not log.debug
- [Estimation = /estimate-issue skill, hours-calibrated](decisions/0007-estimate-issue-skill.md) — issue #17: rubric XS≤1h…XL>16h→split, propose→confirm→write via `issue-board.sh estimate`, calibration log in the skill dir; 0006 is taken by the parked issue-14 branch
- [issue-board.sh args need allow-lists](conventions/issue-board-args-need-allowlist.md) — issue #17: raw CLI args interpolated into jq filters break on `"` — validate against an explicit allow-list (like `estimate` does for SIZE) before calling field_id/option_id
- [Decision comments: cross-issue gh writes blocked](conventions/decision-comments-cross-issue-blocked.md) — issue #15: auto-mode classifier denies `gh issue comment` on any issue other than the one being worked; decision comments target the current branch's issue only; use quoted-heredoc for `--body`

- [Shared board → scope by repo](conventions/shared-board-repo-scoping.md) — issue #12: the "Digital banking" board is multi-repo, so `issue-board.sh` must match cards by `.content.repository` + number (number alone moved another repo's card); pass `--limit 200`
- [Spring Boot 4.1 JPA test quirks](conventions/springboot4-jpa-test-quirks.md) — issue #5: `@DataJpaTest` gone → `@SpringBootTest(NONE)` + `@PersistenceContext EntityManager`; H2 2.4 quotes KEY/VALUE uppercase but Hibernate uses lowercase → `CASE_INSENSITIVE_IDENTIFIERS=TRUE;NON_KEYWORDS=KEY,VALUE`; multi-context Liquibase conflict → separate H2 DB names
- [Spring Boot 4.1 WebMVC test quirks](conventions/springboot4-webmvc-test-quirks.md) — issue #5: `spring-boot-starter-webmvc-test` poisons all contexts via `WebDriverContextCustomizerFactory`; standalone MockMvc + `LocalValidatorFactoryBean`; `authentication()` doesn't set `getUserPrincipal()` in Spring Security 7.x → use lambda post-processor
- [Spring Boot 4 security-testing gotchas](conventions/springboot4-security-testing.md) — issue #4: `@AutoConfigureMockMvc` gone in Boot 4.1 (build MockMvc via `webAppContextSetup(ctx).apply(springSecurity())`); don't tamper JWTs by flipping the last sig char (padding bits → flaky) — splice a foreign payload instead
- [Issue-workflow: board status + memory gate](decisions/0005-issue-workflow-board-and-memory-gate.md) — issue #8: `issue-board.sh` assign/status on Digital banking board + pre-push gate that blocks code pushes lacking `.claude/memory/` changes; `gh` needs `project` scope
- [JaCoCo coverage ratchet + CI gate](decisions/0004-jacoco-coverage-ratchet-and-ci.md) — issue #3: JaCoCo on `verify` + CI, threshold starts at 0.00 ratchet (not 80%) since coverage is ~0%
- [Stop-hook nudge needs commit tracking](conventions/stop-hook-nudge-needs-commit-tracking.md) — dirty-tree-only check went silent for commit-before-stop sessions; also gitignore hook state files
- [Release flow in git-workflow skill](decisions/0003-release-flow-in-git-workflow-skill.md) — develop→release→main process added as step-gated instructions, not a new skill
- [PR template + create-pr skill](decisions/0002-pr-template-and-create-pr-skill.md) — fixed 6-section PR format; requires `gh` CLI installed + authenticated locally
- [Claude Code harness setup](decisions/0001-claude-code-harness-setup.md) — Tier 3 harness: gitflow + conventional commits, workflow gates, sensitive areas, hand-authored memory lifecycle
- [Hook changes need explicit confirm](conventions/hook-changes-require-explicit-confirmation.md) — auto-mode blocks silently wiring new hooks into settings.json
