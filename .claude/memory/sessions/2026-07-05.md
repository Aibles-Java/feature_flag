# Session 2026-07-05

## Issue #24 — hash SDK API keys at rest

- Implemented hashing of SDK API keys: new `ApiKeyHasher` (SHA-256 hex), entity/repo/filter
  switched from plaintext `api_key` to `api_key_hash`, added throttled `last_used_at`.
- Create/rotate now return a dedicated `EnvironmentSecretResponse` (plaintext once); `apiKey`
  removed from `EnvironmentResponse` (breaking API change).
- Migration `009-hash-api-keys.xml`: backfills existing plaintext keys via pgcrypto
  (`dbms="postgresql"`), then drops the plaintext column. Verified end-to-end on real Postgres
  (backfill hash == Java hash) in a rolled-back tx.
- 50/50 tests pass (41 → 50). Security review clean.
- Board: `issue-board.sh start 24` done (assigned + In progress). gh found at
  `~/AppData/Local/gh-cli/bin/gh.exe` (not on PATH).
- Wrote decision 0008 + conventions liquibase-postgres-only-migrations-on-h2 and
  sdk-eval-key-column-h2-500. Remaining: commit, push, PR (`Closes #24`), `ready 24`.
