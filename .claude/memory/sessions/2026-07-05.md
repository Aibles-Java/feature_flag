# Session 2026-07-05

## Issue #26 — rate limiting on auth + SDK endpoints

- Implemented Bucket4j (`bucket4j_jdk17-core:8.19.0`) in-memory rate limiting: `AuthRateLimitFilter`
  (per-IP, `/api/v1/auth/**`) + `SdkRateLimitFilter` (per-env-id, `/api/v1/sdk/**`), 429 + Retry-After
  ProblemDetail, `app.rate-limit.*` config (off in test profile). New package `security/ratelimit/`.
- Hit + fixed two failures: (1) filters must anchor on a *standard* Spring Security filter, not the
  custom auth filters ("does not have a registered order"); (2) the new `@SpringBootTest` context
  collided on the shared H2 `mem:testdb` Liquibase → gave it its own DB URL.
- Code review's one Important finding (unbounded bucket `ConcurrentHashMap`) fixed with **Caffeine**
  `expireAfterAccess=2× refill`. Security review clean. 47/47 tests pass.
- Wrote decision **0009** (used 0009 not 0008 — #24 holds 0008 on its unmerged branch) + conventions
  `spring-security-filter-order-anchor`, `second-springboottest-context-shared-h2`.
- Remaining: commit, push, PR (`Closes #26`), `ready 26`. Follow-ups noted: invalid-key SDK traffic
  unthrottled; distributed (Redis) backend.

## Issue #24 — hash SDK API keys (earlier this session)

- Shipped SHA-256 hashing of SDK keys, migration 009 (pgcrypto backfill), one-time reveal DTO,
  throttled `last_used_at`. 51/51 tests, security review clean, PR **#40** open. Board `ready 24`
  paused by user; `last_used_at` throttle-vs-every-request question left open. (Decision 0008 lives
  on the #24 branch.)
