# 2026-08-25

Started as "how is RBAC planned?" and turned into shipping it. There is no RBAC/ABAC issue in the
repo; grepping the unmerged branches turned up `feature/role` — a single commit from 2026-07-01
holding a **finished** implementation (project grants, custom roles, production protection, change
window) plus `docs/ABAC.md` and a design spec, never merged, never PR'd.

Brought it up to date with `develop` (7 weeks of drift: hashed API keys, refresh tokens, audit log,
pagination, metrics): renumbered its migrations `009–013` → `013–017`, dropped the case-colliding
`docs/ARCHITECTURE.md` so the merge could even start, then resolved 13 Java conflicts by taking
develop's service impls whole and re-applying the ABAC conversion on top. Restored the
`requireRole*` adapters the implementation had deleted but the spec promised — develop's
`AuditService` needs them.

Then closed the two places where the two models genuinely disagreed: `AUDIT_READ` joined the action
vocabulary, and grant / custom-role changes now write audit rows. Wrote ADR-0006, marked ADR-0001's
authorization paragraph superseded, rewrote the permission sections of `CLAUDE.md` and
`architecture.md`, and added two sequence diagrams to `docs/ABAC.md`.

A code read at the end found four more gaps (org read / member list still on `isMember`, no
`updateMemberRole` at all, orphaned grants on project or org delete, and the spec's own test plan
only half delivered) — recorded in `docs/ABAC.md` §12 rather than fixed. 316 tests green; pushed and
PR'd to `develop` with those gaps stated in the PR body.
