# 2026-08-23

## SonarQube CI on self-hosted runner

- Refactored `.github/workflows/sonar.yml`: added `permissions: contents:read`,
  `timeout-minutes:20`, fork-PR guard, `~/.sonar/cache`; dropped redundant
  `-Dsonar.host.url/-Dsonar.token` (plugin auto-reads the env vars).
- Clarified for the user: no `.env` needed — `SONAR_TOKEN`/`SONAR_HOST_URL` are GitHub
  **repo secrets**; the workflow itself is the Sonar integration (no separate "link").
- Walked through registering a self-hosted GitHub Actions runner on the internal server
  (dedicated user, `run.sh` for first test → `svc.sh` service later). Runner is online/Idle.
- Committed workflow as `4f2b690`, moved onto branch `feature/sona-ci`. Push was blocked
  by the memory gate → ran `/save-memory` (this) to record decision 0018 and unblock.
