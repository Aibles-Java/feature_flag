# 2026-07-20

## Issue #32 — refresh tokens: implemented all 7 tasks (Tasks 1–7), not yet pushed

- Executed the existing plan (`docs/superpowers/plans/2026-07-18-refresh-tokens.md`) task by task:
  JWT TTL split (15min/14d) → `RefreshToken` entity + migration 010 → repository (atomic
  conditional-UPDATE `consume`) → `RefreshTokenService` rotation/reuse-detection → auth
  DTOs/`AuthService` → `/auth/refresh` + `/auth/logout` endpoints → docs + verify.
- Found **two real bugs the plan's mock tests couldn't catch**, both about the family revoke
  actually committing: (1) revoke-then-throw is undone by the rollback → fixed with a separate
  `REQUIRES_NEW` bean, not `noRollbackFor` (which any `@Transactional` caller defeats); (2) revoking
  while `consume()` holds a row lock self-deadlocks on PostgreSQL → check `user.enabled` before
  `consume()`. Both captured in `decisions/0017`. Verified each empirically by reverting the fix and
  watching only the right test fail.
- Ran the `security-review` skill: 3 findings, all filtered <8 confidence by parallel adversarial
  sub-agents (sliding-lifetime & error-oracle are documented design choices; deadlock is DOS not
  bypass). Fixed the deadlock anyway (`433266f`) as a reliability bug.
- Resolved the `docs/ARCHITECTURE.md`/`architecture.md` Windows case collision → restored the v1.0
  design doc at `docs/architecture-design-v1.md` (`cba9edb`), clearing an old HANDOFF follow-up.
- `./mvnw verify`: 239 tests green, Spotless clean, JaCoCo met. **Next: push + PR (headline the
  BREAKING `token`→`accessToken` response change) + board `ready 32`.**
