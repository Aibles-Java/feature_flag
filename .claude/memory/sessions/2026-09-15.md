# 2026-09-15

Read the project, the API design doc and the API key implementation, then reviewed and
hardened the latter at the user's request.

- Synced `docs/architecture-design-v1.md` §4/§6/§7 with the code — it still claimed API
  keys were stored in plaintext, two filter chains, a 24h JWT and pagination as v2 work.
- Reviewed the API key area at effort `high`. The key handling itself (generate, hash,
  store, rotate) was clean; seven findings landed elsewhere. Fixed the three that were
  genuinely on the API key *authentication* path, TDD throughout: unguarded
  `last_used_at` write, anonymous key probing with no rate limit, off-schema 401 body.
  New `SdkIpRateLimitFilter`; `RateLimitService` moved to an `EnumMap` per scope. Security
  review of the change: no HIGH/MEDIUM. 470 → 478 tests.
- Fixed the one HIGH finding on its own branch: creating an environment never backfilled
  `FlagEnvironmentState`, so flags predating it were invisible to the SDK and untoggleable.
  Added migration `019` to repair existing data.
- Discovered `feature/api-key-lifecycle` rewrites the same filter and carries all three
  auth defects forward — a merge collision that needs resolving whichever side lands
  second.
- Left open: three `EnvironmentServiceImpl` findings (ADMIN can set `type=PRODUCTION` at
  create and lock project-wide archive; duplicate rename returns 500 not 409; a change
  window cannot be cleared).
