# 2026-09-03

Started from the question "is the project fully ABAC yet?" — answer: `develop` is pure RBAC, and
the ABAC work sat unmerged on `feature/role` / PR #87, `CONFLICTING` and 39 commits behind.

Merged `origin/develop` into `feature/role` in a worktree (5 union conflicts; migrations
renumbered `013–017` → `014–018`, memory decision `0023` → `0034`), then closed the production
protection hole reading the branch turned up: rule B/D only ever guarded `FLAG_STATE_UPDATE`, so
archive, key rotation and env delete each changed production unchecked — archive worst of all,
since archived flags are filtered out of every SDK response.

The code-reviewer agent then found a fourth path the first fix had missed: `importSnapshot`
(issue #38, merged in from develop) was still on the `requireRoleForEnvironment` adapter and wrote
flag state straight into the target environment. Verified by reading the code before acting, then
fixed by authorising import as `FLAG_CREATE` + `FLAG_STATE_UPDATE` on the environment.

455 → 470 tests, 0 failures. Three commits on `feature/role`, docs and ADR-0006 updated.
