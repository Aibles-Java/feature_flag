# 2026-07-15

## Session: implement issue #33 — pagination on admin list endpoints

- Read #33; branched `feature/issue-33-pagination-admin-list` off latest develop; board → In progress.
- Explored the codebase (Explore agent): mapped 6 unbounded admin list endpoints
  (orgs listMine, members, projects, environments, active + archived flags) + their
  controller→service→repo→DTO chains and tests; confirmed SDK eval endpoint to exclude.
- Implemented offset pagination: `PageResponse<T>` envelope, `PaginationConfig` max-100 clamp bean,
  controllers `@ParameterObject @PageableDefault(size=20, sort={createdAt,id})`, services return
  `Page<>`, repositories gained paginated methods (overloaded where a `List` caller remained, e.g.
  `EnvironmentRepository.findAllByProjectId` still List for flag-create). `listMine` via
  `findByIdIn(ids,pageable)` (empty→Page.empty). Wrote `docs/adr/ADR-0003-pagination-strategy.md`
  (offset over cursor). → `decisions/0017`.
- Updated 9 existing list tests to the envelope shape; added clamp-to-100 / default-20 /
  deterministic-sort controller tests and a real-H2 repo paginate+sort test.
- `./mvnw verify` green: 214 tests, Spotless clean, coverage met. code-reviewer agent: no
  CRITICAL/HIGH/MEDIUM.
- **Code review surfaced bug #52's root cause**: `listMembers` reads lazy `getUser().getEmail()`
  outside a tx (open-in-view=false) → LazyInit 500. Pre-existing; deliberately left for #52 to keep
  #33 scope-pure (needs a real-DB integration test to fix+verify). Recorded in HANDOFF + 0017.
- Also this session earlier: shipped #28 (PR #54) and read the SDLC roadmap doc; saved
  `gh-cli-off-path-location` memory (gh at `C:\Users\ACER\AppData\Local\gh-cli\bin`, not on PATH).

## Session (cont.): implement issue #31 — append-only audit log
- Branch `feature/issue-31-audit-log` **stacked on #33** (read endpoint needs #33's PageResponse).
- Built: `audit_log` (migration 010 → renumbered **011** at the develop merge, `${json.type}` jsonb/json), `AuditLog` entity + 2 enums,
  `AuditLogRepository`, `AuditService` (SYNC same-tx write + paginated read), `AuditController`.
  Wired 16 `record()` calls into the 4 service impls. Key design: synchronous same-transaction
  audit (atomic, exactly-one-row) — NOT the async Slack event pattern.
- De-risked first: validated `@JdbcTypeCode(SqlTypes.JSON)`+`Map` JSON round-trip on real H2 before
  wiring. Hit + fixed: **Boot 4.1 has no ObjectMapper bean in webEnvironment=NONE** → AuditService
  builds its own mapper. Never audits secrets (env create uses non-secret DTO; key rotation
  before/after=null, tested). → `decisions/0020` (authored as 0018; 0018/0019 are claimed by open PRs #60/#43).
- `./mvnw verify`: 221 tests, Spotless clean, coverage met. code-reviewer: no findings.
