# 2026-08-12

## Issue #48 — ArchUnit Tier-1 governance gate

Implemented Track A of the code-graph spec: `archunit-junit5:1.4.2` (test scope) plus
`architecture/ArchitectureTest` with rules R1–R7, gating through Surefire in `./mvnw verify` with
no plugin or CI change. `./mvnw verify` green — 257 tests, Spotless clean, JaCoCo floor met.

The spec's §A.4 code sketch needed three corrections against the current code: R4's
`accessClassesThat()` form doesn't compile on 1.4.2, R1 needs `Security`/`Config` declared as
layers (auth filters legitimately read repositories pre-service), and R4 must allow the whole
`security` package since ArchUnit can't tell a `SecurityContextHolder` read from a write. R7 is
frozen against a committed baseline for the pre-existing `config` ↔ `security` cycle.

Ran the acceptance negative test in both directions and reverted: a controller → repository
reference failed R1+R2 under full `verify`; a fresh package cycle failed the *frozen* R7 without
mutating the store. First attempt at the negative test was a false signal — an injected `private
final` field got swept into Lombok's `@RequiredArgsConstructor` and broke test compilation before
the rule ran.

Shipped as ADR-0004 (issue and spec both say 0003, which pagination already holds) and backfilled
the missing ADR-0003 row in the ADR index.

---

# 2026-08-13

## Issue #48 review round

Reviewed the Tier-1 gate specifically for **vacuous** rules and found two: R6 filtered on
`haveSimpleNameEndingWith("Repository")` (a hand-rolled DAO under any other name slipped through)
and R4's `EvaluationController` carve-out was dead — that controller takes an injected
`Authentication` parameter. `CLAUDE.md`'s own claim that it reads `SecurityContextHolder` was wrong
and was corrected. Also flipped `allowStoreCreation` to false after confirming a missing store made
the frozen R7 silently re-baseline and report green.

## Issue #49 — Tier 2

Both conditions the issue specifies are vacuous as written, proven with `javap`: Lombok's `@Setter`
puts the only `putfield key` sites inside `FeatureFlag`, and `UpdateFeatureFlagRequest` has no
`key` field so `getKey()` doesn't exist. Shipped R8 (custom `ArchCondition` catching field-SET *and*
`setKey` calls from outside the entity) and R9 (no `key` field on the update DTO). Both negative-
tested. Branch stacked on #48.

## Ran the frontend

`feature_flag_ui` on :5173 (needed `npm install` first), rendered and screenshotted via headless
Chrome. Proved FE→BE connectivity with a real browser request from the FE origin — which surfaced
a pre-existing backend bug: login with bad credentials returns 500 instead of 401, because
`GlobalExceptionHandler` has no `AuthenticationException` handler. Not fixed; recorded in HANDOFF.
