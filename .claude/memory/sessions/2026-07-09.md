# 2026-07-09

## Issue #29 — Micrometer + Prometheus metrics

- Read the task; flagged that the issue's "Depends on: Actuator issue" (#25/PR #42) was still
  OPEN and not on `develop`. Per the user, treated #29 as independent: branched off `develop`
  and added actuator itself (self-contained).
- Implemented: actuator + micrometer-prometheus deps; `FeatureFlagMetrics` façade with bounded
  meters (`ff_evaluations_total{environment}` + latency timer, `ff_flag_changes_total`,
  `ff_auth_failures_total`) eager-registered at 0; instrumented eval service, flag service, and
  both auth filters. New `@Order(0)` management security chain: `/actuator/**`, health public,
  prometheus/info behind HTTP-Basic `METRICS` role (local in-memory scraper user).
- Security review caught a real MEDIUM in my own code: blank `app.metrics.password` +
  `NoOpPasswordEncoder` = empty-password auth bypass. Fixed by disabling the account when the
  secret is blank; added `PrometheusBlankPasswordIntegrationTest`.
- Full suite 172/172 green. Saved decision 0012 + convention `actuator-management-chain-boot41`.
  Next: commit → push → PR (`Closes #29`) → board *Ready For Testing*.
