# Session 2026-07-13

## Issue #30 — Evaluation caching (Caffeine) + ETag/304

Implemented full P1 caching feature on branch `feature/issue-30-evaluation-caching`.
Cache stores `FlagStateSnapshot` (pre-rollout) by `environmentId` using raw Caffeine (same pattern as rate-limiter).
ETag/304 handled via `ShallowEtagHeaderFilter` FilterRegistrationBean — zero controller changes needed.
Eviction wired into `FeatureFlagServiceImpl` for all four mutating paths (create fans out to all project envs; updateState targets single env).
Micrometer gauges registered via `recordStats()` on the Caffeine builder.
Key gotcha: Spring Boot 4.x removed `@SpyBean` — now `@MockitoSpyBean` from `org.springframework.test.context.bean.override.mockito`.
All 186 tests pass, `./mvnw verify` green (Spotless + JaCoCo ≥83%). PR not opened yet.
