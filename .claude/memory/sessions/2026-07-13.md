# 2026-07-13

## Session: implement issue #28 — structured JSON logging with request correlation

- Read issue #28 via WebFetch (`gh` not installed on this machine). Switched to `develop`,
  fast-forwarded 25 commits, branched `feature/issue-28-structured-json-logging`.
- Implemented all 4 acceptance criteria:
  - `logging/RequestCorrelationFilter` (+ `MdcKeys`), registered in `config/LoggingConfig` via
    `FilterRegistrationBean` at `HIGHEST_PRECEDENCE` → runs before the single `FilterChainProxy`
    (@−100), i.e. before all 3 security chains. Sets `requestId` (MDC/attr/`X-Request-Id` header),
    clears the WHOLE MDC in `finally` = single choke point; `X-Request-Id` sanitized `[A-Za-z0-9_-]{1,64}`.
  - `userId`/`envId` added by the two auth filters (no own cleanup — outer filter clears).
  - `GlobalExceptionHandler.withRequestId()` on every handler (only when MDC id non-null).
  - `logback-spring.xml`: prod=LogstashEncoder JSON / dev=`[req=%X{requestId}]`; dep
    `logstash-logback-encoder:8.0` runtime.
- Tests: filter (incl. **cross-request leakage** + clear-on-throw), exception-handler, and a
  prod-encoder-config guard (class resolves + prod profile wires it). `./mvnw verify` green:
  195 tests, Spotless clean, JaCoCo 0.83 met. code-reviewer agent: **no findings**.
- → `decisions/0015-structured-json-logging-request-correlation.md`.
- Discovered pre-existing repo issue: `docs/ARCHITECTURE.md` vs `docs/architecture.md` are two
  git-tracked paths differing only by case → git perpetually reports one as modified on Windows.
  Kept out of the #28 staged set. Noted in HANDOFF.
- **Not yet committed/pushed** at time of writing; PR + board `start`/`ready` steps blocked on
  `gh` being installed. Board step 1 (In progress) was skipped.
