# Session 2026-07-08

## Issue #25 wrap-up (PR #42)

- Reviewed PR #42; merged `develop` (rate-limiting #26) into the #25 branch to clear the
  predicted `SecurityConfig.java` conflict (kept both), pushed → PR #42 MERGEABLE + CI green.
- Fixed a merge side-effect: the resolve took develop's `MEMORY.md`/`HANDOFF.md`, dropping the
  `0010` index line — restored. New convention `permitall-does-not-skip-servlet-filters`.

## Issue #27 — Docker port + non-root (this branch)

- Branched `feature/issue-27-docker-port-nonroot` from **fresh develop** (user chose: do NOT
  merge #25, implement #27 independently).
- `Dockerfile` EXPOSE 8080→8081 + non-root `spring` user; `docker-compose` new `app` service
  (DB via service name, 8081:8081, no healthcheck until actuator #25); CLAUDE.md :8080→:8081.
- Verified end-to-end in Docker: whoami=spring, `Tomcat started on port 8081`, `:8081/api-docs`
  →200. Decision **0011** (skipped 0010 — held by #25).
- Hit the Windows `docs/ARCHITECTURE.md` vs `docs/architecture.md` case-collision (perpetual
  dirty tree) → convention `windows-docs-case-collision`; committed by staging explicit paths.
