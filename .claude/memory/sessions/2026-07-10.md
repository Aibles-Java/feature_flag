# 2026-07-10

## Harness-improvement session

Reviewed the existing harness, then implemented four enforcement gaps (decision 0012):

1. **Liquibase immutable-migration guard** — PreToolUse hook blocks edits to existing
   changesets under `db/changelog/migrations/`; new files pass. Tested: existing→exit 2,
   new→exit 0, non-migration→exit 0.
2. **Security-review gate** — Stop hook nudges `/security-review` when `security/`,
   `SecurityConfig`, `ApiKeyGenerator/Hasher`, or `db/changelog/` change; de-duped per day.
3. **Spotless** (google-java-format) added to `pom.xml`, `spotless:check` bound to `verify`
   (existing CI enforces it); Stop hook `format-changed.sh` auto-formats changed files.
4. **coverage-floor.sh** — measures real JaCoCo coverage and ratchets the pom floor (0.00 today).

Wired the 3 hooks into `settings.json`; updated CLAUDE.md (code-style + two workflow bullets).
Validated JSON/XML and all script syntax. **Blocker:** no JDK on this machine's PATH, so the
two rollout commands (`./mvnw spotless:apply`, `coverage-floor.sh --apply`) are left for the
user — hooks self-skip when mvnw can't run. Nothing committed yet.

## Slack notifications session

Scoped a Slack notification plan (matrix in `docs/slack-notifications.md`) and implemented it on
`feature/slack-notifications` (commit 0961d7c) — see decision 0013. Three mechanisms: GitHub
Slack app (CI/PR/release, config-only), `issue-board.sh ready` webhook (opt-in), and in-app
events via `@Async @TransactionalEventListener(AFTER_COMMIT)` → fail-safe `SlackNotifier` for
flag toggle/archive/key-rotation. Delegated the Java build to an executor, then verified myself:
`./mvnw spotless:check test` → **170 tests green**. Security review: 0 crit/high/med; applied two
LOW fixes (catch logs exception class not message to avoid webhook-URL-in-logs; `.omc/` gitignored).

**Correction:** Java 21 (temurin) + Maven 3.9.16 **are now installed** on this machine — the
"no JDK" blocker noted in the harness session above is stale; the build runs locally now.

Helped the user set up Slack: webhook create/test, `APP_SLACK_ENABLED`+`SLACK_WEBHOOK_URL` env
vars, GitHub `/github subscribe`. Their "error, no noti" was **Postgres not running** — app dies
at startup without `docker compose up -d`, unrelated to Slack. Next: push + PR (also live-tests
the GitHub→Slack app).
