# 2026-07-01

- Ran `/shipwithai-starter:init` (Tier 3): CLAUDE.md sections, `docs/architecture.md`,
  `docs/adr/`, `docs/CODEMAPS/`, permissions preset, `git-workflow` skill, `drift-monitor`
  agent, observability hook.
- Hand-authored the `.claude/memory/` lifecycle (store + load/save hooks + `/save-memory`
  skill) since the plugin's asset templates were missing.
- Researched PR-automation options; nothing existing enforced a fixed format, so built
  `.github/PULL_REQUEST_TEMPLATE.md` + `.claude/skills/create-pr` skill instead.
- Created branch `feature/claude-harness-setup` off `develop`, committed all of the above in
  two commits, pushed, and opened **PR #1** (https://github.com/Aibles-Java/feature_flag/pull/1)
  using the new `create-pr` format after the user installed and authenticated `gh` CLI.
- See `decisions/0001-*` and `decisions/0002-*` for the full rationale.

## Session 2

- Designed a develop→release→main release/hotfix process; brainstormed with the user
  whether it should be a new skill vs. an extension of `git-workflow`, and how autonomous
  the agent should be — resolved via `AskUserQuestion` (extend `git-workflow`; confirm only
  at risky steps: merge-to-main, tag, merge-back).
- Implemented: `git-workflow` skill's new "Release flow" section, `docs/adr/ADR-0002-release-process.md`.
- Branch `feature/release-flow-skill` off `develop`, opened **PR #2**
  (https://github.com/Aibles-Java/feature_flag/pull/2) via `create-pr` skill, first PR of the
  session confirmed with the user before creation.
- User reported the save-memory Stop-hook reminder "not working." Root cause: it only
  checked for a dirty git tree, and this repo's normal flow commits+pushes before stopping,
  so it never fired. Fixed `remind-save.sh` to also track last-seen commit sha; also found
  and fixed a related gitignore gap (hook state files were untracked and unignored, which
  would have kept the tree permanently "dirty" once created). Fix verified with manual
  hook invocations, then committed onto the same PR #2 branch.
- See `decisions/0003-*` and `conventions/stop-hook-nudge-needs-commit-tracking.md` for
  full rationale.
