# 2026-08-26

## Merge SonarQube into the CI/CD pipeline (gated behind the build)

- Switched from `fix/trivy-action-version` to `develop` (stashed an unrelated
  loopback-binding change to `docker-compose.prod.yml` → `stash@{0}`).
- Merged the standalone `.github/workflows/sonar.yml` into `workflow.yml` as a `sonar`
  job with `needs: test`, so Sonar only runs once the CI build is green instead of in
  parallel. Kept the job self-contained (self-hosted, own `verify`); deleted sonar.yml.
  Preserved the exact triggers and the `SonarQube analysis` job name (branch protection).
- `actionlint` clean. Committed on `feature/merge-sonar-into-ci` as `f523d07` (amended once
  — first commit missed the workflow.yml edit due to a `git add` pathspec abort).
- Recorded decision [[0024-merge-sonar-into-ci-gated]]. Next: push + PR into develop.

## Gate GHCR publish behind Sonar (follow-up, after PR #88 merged to develop)

- User merged the 0024 branch to develop (PR #88), then asked to also gate `publish` on
  Sonar. Created `feature/gate-publish-behind-sonar` off `origin/develop`.
- `publish` now `needs: [test, sonar]`; rewrote its `if:` to `!cancelled()` + explicit
  result checks so it runs on Sonar `success`/`skipped` but blocks on `failure/cancelled`
  — needed because `sonar` is skipped on tag pushes and a skipped dep would otherwise skip
  publish and break `v*` releases. `deploy` inherits via `needs: publish`.
- `actionlint` clean. Commit `f6afcec`. Recorded decision [[0025-gate-publish-behind-sonar]].
