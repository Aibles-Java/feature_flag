# Session: 2026-07-02

## Session 1 — issue #15: Decision-comment convention

- Worked issue #15 end-to-end via `issue-workflow`: board card → In progress, branch
  `feature/issue-15-decision-comments` off `origin/develop`.
- Added a **Decision comments (human-in-the-loop)** section to the `issue-workflow`
  skill: templated 🧑‍⚖️ comment (Q / Options / Chosen — rationale) posted to the linked
  issue after a substantive decision resolves; substantive-only, sanitized (public
  repo), skip silently when no issue is linked, no duplication with memory/PR body.
- code-reviewer pass caught that a single-quoted `--body` breaks on apostrophes →
  switched the documented command to the quoted-heredoc pattern from `create-pr`.
- Verification checkbox left open: no live decision this session; retroactive comment
  to #4 blocked by the auto-mode classifier (recorded as a new convention memory).
- Also hit a ~5-minute Bash/WebFetch safety-classifier outage at session start;
  bridged it with a background timer and read-only prep.

## Session 2 — issue #14 research: self-hosted SonarQube decision

- Researched #14 (coverage dashboard): repo public, JaCoCo xml/csv already produced on
  `verify`, ratchet still 0.00, **no README.md exists** (issue's badge criterion
  assumes one), issue's `codecov-action@v4` is dated (v5 current).
- Human decided **self-hosted SonarQube** over Codecov/SonarQube Cloud; infra in a
  separate repo. Posted the templated Decision comment to #14 — **first real
  verification of the #15 convention**.
- Ticking #15's last acceptance checkbox was classifier-blocked (`gh issue edit` is
  cross-issue too) → convention addendum written; human to tick it.
- Assessed pre-server feasibility; agreed plan recorded in `decisions/0006`. Work
  parked on `feature/issue-14-sonarqube-coverage-board` (memory-only push) until the
  SonarQube infra repo exists.
