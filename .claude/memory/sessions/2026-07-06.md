# Session 2026-07-06

## Issue #25 — Actuator health / liveness / readiness

- Added `spring-boot-starter-actuator`; exposed only `health,info`; enabled probes; readiness
  includes `db`, liveness process-only; `show-details=never`. Permitted `/actuator/health/**`
  anonymously in `SecurityConfig` (admin chain), everything else stays authenticated.
- Dockerfile: `HEALTHCHECK` on `/actuator/health/readiness`; fixed `EXPOSE 8080→8081` to match
  `server.port`.
- Tests in `SecurityChainIntegrationTest`: health/liveness/readiness → 200 anonymous, `/actuator/info`
  → 403. 54/54 pass. Security review clean (info.env only surfaces info.*, no secret leak).
- Decision **0010** (0009 held by unmerged #26). Remaining: commit, push, PR (`Closes #25`), `ready 25`.

## #26 integration (earlier today)

- #24 (PR #40) merged to develop → PR #41 (#26 rate limiting) went CONFLICTING. Merged develop into
  the #26 branch, resolved 3 memory-file conflicts, fixed `RateLimitIntegrationTest` to the hashed
  API (`apiKeyHash(ApiKeyHasher.hash(...))`). 57/57 pass, pushed → PR #41 now MERGEABLE + CI green.
