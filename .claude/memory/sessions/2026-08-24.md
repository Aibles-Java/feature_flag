# 2026-08-24

## Trivy gate red on branch `fix/trivy-action-version` — real CVE, not config

- Investigated failing CI run 32654490354. The `aquasecurity/trivy-action` version pins
  from earlier commits worked — the scan step now runs to completion.
- The failure was a **genuine HIGH**: `org.postgresql:postgresql` 42.7.11 flagged for
  CVE-2026-54291 (scram-client), fixed in 42.7.12. Gate config (`exit-code:1`,
  `ignore-unfixed:true`) correctly blocked it.
- The driver version is managed by `spring-boot-starter-parent` 4.1.0, so fixed by
  overriding `<postgresql.version>42.7.12</postgresql.version>` in `pom.xml <properties>`
  rather than pinning an explicit `<dependency>` version.
- Verified: `mvn help:evaluate -Dexpression=postgresql.version` → 42.7.12; `./mvnw clean
  package` green on H2. Committed `94d4f5b`. Recorded decision **0023**.
