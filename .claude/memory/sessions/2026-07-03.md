# Session 2026-07-03

## Issue #10 — JWT 500 → 403 for deleted user

Fixed `JwtAuthenticationFilter.doFilterInternal`: wrapped user-lookup in try/catch for
`UsernameNotFoundException` (log.warn, fallthrough unauthenticated) and `JwtException`
(log.debug, guards TOCTOU window between validateToken and getEmailFromToken). Added `@Slf4j`.
Flipped pinned-defect integration test to assert `isForbidden()`. Added unit test for catch path.
Code reviewer flagged log.debug→warn and missing JwtException catch; both fixed. All 41 tests pass.
Branch `feature/issue-10-jwt-deleted-user-500` ready to commit/push/PR.
