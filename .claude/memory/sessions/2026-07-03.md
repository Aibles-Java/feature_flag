# Session 2026-07-03

## Issue #10 — JWT 500 → 403 for deleted user

Fixed `JwtAuthenticationFilter.doFilterInternal`: wrapped user-lookup in try/catch for
`UsernameNotFoundException` (log.warn, fallthrough unauthenticated) and `JwtException`
(log.debug, guards TOCTOU window between validateToken and getEmailFromToken). Added `@Slf4j`.
Flipped pinned-defect integration test to assert `isForbidden()`. Added unit test for catch path.
Code reviewer flagged log.debug→warn and missing JwtException catch; both fixed. All 41 tests pass.
Branch `feature/issue-10-jwt-deleted-user-500` ready to commit/push/PR.

## Session 2 — 2026-07-03 (evening continuation, picked up from context-compacted prior session)

- Completed remaining code-reviewer findings for issue #5: added `update` + slug-validation
  tests to `OrganizationControllerTest`, `create_returns400_whenOrganisationIdIsNull` to
  `ProjectControllerTest`, and `update_changesName` + `listMembers_throwsUnauthorized` to
  `OrganizationServiceImplTest`; removed dead `findById` stub from `EnvironmentServiceImplTest`.
- Fixed `NullPointerException` in 3 `FeatureFlagServiceImplTest` tests: `FlagEnvironmentState`
  entities needed an `Environment` set so `toStateResponse()` could call `.getEnvironment().getId()`.
- All 147 tests pass. Two new convention files written: `springboot4-jpa-test-quirks.md`
  (H2 2.4 KEY/VALUE case mismatch, `@DataJpaTest` removed, multi-context Liquibase conflict)
  and `springboot4-webmvc-test-quirks.md` (standalone MockMvc, `authentication()` principal
  gap, `WebDriverContextCustomizerFactory` side-effect). Ready to commit + push.
