# 2026-08-05

## Session: resolve three PR conflicts, then implement issue #35

- **Conflict-resolved PRs #43, #58, #60** (all now MERGEABLE/CLEAN, CI green). Common blocker: the
  Windows `docs/ARCHITECTURE.md` case-collision makes `git merge` refuse to start; `git rm --cached`
  is the fix, stashing is not. Non-git-visible collisions were the real finds — **#58's migration
  `010` clashed with #32's `010-create-refresh-tokens`** (git merged both silently, different
  filenames), and **#43's compose service would have crash-looped** because develop's Dockerfile now
  bakes `SPRING_PROFILES_ACTIVE=prod` with no default for `APP_JWT_SECRET`. On #60 verified the
  raised 0.87 coverage floor still holds post-#32 (0.8938), and measured #58 against it too (0.9099)
  rather than leaving a merge-order caveat open.
- Answered #58's "check the warning please": diffed its CI warnings against a `develop` run — **none
  are introduced by that PR**; only the `H2Dialect` one is worth fixing. Awaiting reviewer.
- **Issue #35** (percentage rollout): discovered ~90% was already on `develop` via
  `feat/rollout-percent` — the issue was never closed. → `conventions/stale-issue-scope-verify-…`.
- Real work delivered: found and fixed a **`Math.abs(Integer.MIN_VALUE)` sign bug** giving a negative
  bucket (one identifier permanently in every rollout); wrote the 24-test `RolloutEvaluatorTest` the
  ACs demanded (determinism, monotonicity, chi-square uniformity over 10k, per-flagKey independence);
  wrote **ADR-0004** pinning the fail-open contract and the pre-rollout cache invariant. TDD order:
  added the `toBucket` seam, watched the regression test fail, then fixed. → `decisions/0021`.
- `./mvnw clean verify`: 274 tests, 0 failures, Spotless clean, coverage met. Branch not yet pushed.
