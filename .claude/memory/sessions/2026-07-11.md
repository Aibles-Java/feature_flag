# 2026-07-11

## Code graph adoption — research, spec, issues
- Researched code graphs (concept, core strength = transitive/negative/aggregate/cross-edge
  queries grep+embeddings+LSP can't express) and the Java/Maven tool landscape.
- Decided: **ArchUnit** for governance (Goal A) + **CodeGraphContext MCP** for agent queries
  (Goal B); jQAssistant/Joern deferred, Sourcegraph/build-own rejected. → `decisions/0014`.
- Wrote `docs/specs/codegraph-adoption.md` incl. a Mermaid solution-design diagram (4-colour
  scheme); render-verified valid via mermaid-cli; self-verified component/edge colouring.
- Filed issues #48 (M/5h), #49 (S/3h), #50 (S/2h) on the Digital banking board with estimates;
  cross-linked deps; logged to `calibration.md`.
- Gotcha found: `issue-board.sh estimate` fails on run 1 for a new issue (Projects API eventual
  consistency after `item-add`) → re-run works. → `conventions/issue-board-estimate-eventual-consistency.md`.
- Branch `feature/codegraph-adoption`; committing spec + estimates + memory, then push + PR into develop.
